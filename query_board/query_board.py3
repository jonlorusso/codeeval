#!/usr/bin/env python

import sys

test_cases = open(sys.argv[1], 'r')

def set_row(rows, row, x):
    for col in rows[row]:


for test in test_cases:
    rows = []

    SetRow
    SetCol
    QueryRow
    QueryCol

test_cases.close()
