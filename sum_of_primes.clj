#!/usr/bin/env clojure

(defn fizzbuzz [a b i]
  (let [fizz (= 0 (mod i a))
        buzz (= 0 (mod i b))]
    (if (and fizz buzz) "FB"
      (if fizz "F"
        (e 'clojure.java.io)

(defn -main [& args]
  (println "Foo"))

